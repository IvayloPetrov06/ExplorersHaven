@using Explorers_Haven.Models
@model IEnumerable<Activity>

<partial name="_Notification"></partial>

<div class="container">
    <div class="row">
        <div class="col col-md-6">
            <h3>Събития</h3>
        </div>
        <div class="col col-md-6">
            <a asp-controller="Activity" asp-action="AddActivity" class="btn btn-primary">Добави Събитие</a>
        </div>


    </div>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">ИД</th>
            <th scope="col">Име</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var act in Model)
        {
            <tr>
                <th scope="row">1</th>
                <td>@act.Id</td>
                <td>@act.Name</td>
                <td>
                    <div class="btn-group">
                        <a asp-controller="Activity" asp-action="EditActivity" asp-route-Id="@act.Id" class="btn btn-primary">Редактирай</a>
                        <a asp-controller="Activity" asp-action="Delete" asp-route-Id="@act.Id" class="btn btn-primary">Изтрий</a>
                    </div>

                </td>
            </tr>
        }
    </tbody>
</table>
