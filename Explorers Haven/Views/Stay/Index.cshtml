@model StayViewModel
<div class="text-center">
    <h1 class="display-4">Престои</h1>
    <p>Хиляди хотели!</p>
</div>

<h2>Търсете престои</h2>
<form asp-action="Index" method="get" class="mb-4">

    <div class="form-group">
        <label for="Name">Име</label>
        <input type="text" asp-for="Name" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Покажи</button>

</form>


<h2>Всички налични престои</h2>
<table class="table tabl-striped">
    <thead>
        <tr>
            <th>Име</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Stays.Count > 0)
        {
            @foreach (var travelogue in Model.Stays)
            {
                <tr>
                    <td>@travelogue.Name</td>
                </tr>
            }
        }
    </tbody>

</table>
