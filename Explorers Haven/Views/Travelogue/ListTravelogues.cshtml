@using Explorers_Haven.Models
@model IEnumerable<Travelogue>

<partial name="_Notification"></partial>

<div class="container">
    <div class="row">
        <div class="col col-md-6">
            <h3>Пътеписи</h3>
        </div>
        <div class="col col-md-6">
            <a asp-controller="Travelogue" asp-action="InsertForms" class="btn btn-primary">Добави пътепис</a>
        </div>


    </div>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">ИД</th>
            <th scope="col">Име</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var trav in Model)
        {
            <tr>
                <th scope="row">1</th>
                <td>@trav.Id</td>
                <td>@trav.Name</td>
                <td>
                    <div class="btn-group">
                        <a asp-controller="Travelogue" asp-action="EditTravelogue" asp-route-Id="@trav.Id" class="btn btn-primary">Редактирай</a>
                        <a asp-controller="Travelogue" asp-action="Delete" asp-route-Id="@trav.Id" class="btn btn-primary">Изтрий</a>
                    </div>

                </td>
            </tr>
        }
    </tbody>
</table>